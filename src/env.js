let cartItemList = [];

const result = cartItemList.filter(c => c.id === 1);

if (result.length > 0) {
  console.log("nada: ", result);
} else {
  console.log("Rsult: ", result);
}

//Error After clicking Items in ItemList, RESET Qt
//Error After clicking Items in ItemList, RESET Qt
//decrement item qt till 0 only
